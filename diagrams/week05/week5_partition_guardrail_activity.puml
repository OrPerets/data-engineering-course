@startuml
!include ../_template_styles.puml

start
:Parse BI query;
if (Has date_key predicate?) then (yes)
  :Allow execution;
  :Prune partitions;
  :Track bytes scanned;
else (no)
  :Reject query;
  :Return guardrail hint;
endif
stop

@enduml
